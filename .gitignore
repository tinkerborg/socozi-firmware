# Build output — regenerable.
build/

# Flash dumps from `make backup`. factory-firmware.bin is deliberately not
# ignored: it is the recovery image.
backup/

.DS_Store

# ESPHome build products — generated from the firmware build.
esphome/components/socozi/socozi_layout.h
esphome/components/socozi/socozi_image.h
esphome/.esphome/
esphome/secrets.yaml
